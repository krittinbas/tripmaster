import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const primaryColor = Color(0xFF000D34); // น้ำเงินเข้ม
  static const secondaryColor = Color(0xFF6B7280); // สีเทา

  // Background colors
  static const backgroundColor = Colors.white;
  static const buttonBackground = Color(0xFFF4F4F5); // สีพื้นหลังปุ่มสีเทาอ่อน

  // Text colors
  static const primaryText = Color(0xFF000D34);
  static const secondaryText = Color(0xFF6B7280);
  static const usernameText = Color(0xFF64748B);

  // Prevent instantiation
  AppColors._();
}
